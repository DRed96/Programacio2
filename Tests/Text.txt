/*Examen Parcial Programació 2 (24 Març 2015)

			Crea una classe “Projectile” que fent servir la classe Punt2D i coneixent una velocitat pugui calcular la posició del punt donat un temps t(2 punts)
			Afegeix un mètode “removeWastedMemory” al Array Dinàmic que elimini tota la memòria que no es faci servir.Ha de retornar els espais de memòria alliberades. (2 punts)
			Afegeix un mètode “delNodes” a la classe de llista doblement enllaçada per poder esborrar qualsevol nombre de nodes des d’una posició qualsevol.Ha de retornar la quantitat de nodes eliminats. (3 punts)
			Afegeix dos mètodes “prefix” a la classe cadena per poder afegir un altre classe cadena o cadena de c al principi.Ha de retornar a si mateixa. (3 punts)

			Criteris de correcció :
		Si no compila o el unit test no passa el exercici no es corregira
			Minimitzar crides als new i delete
			Codi const - correct
			Codi clar i ben organitzat

			Unit Test a passar(feu copiar i enganxar)
			
			DLinked list
	
	/*const bool Pop()
	{
	if (start != NULL && end != NULL)
	{
	if (start == end)
	{
	size = 0;
	start = end = NULL;
	}
	else
	{
	end = end->prev;
	end->next = NULL;
	size--;
	}
	return true;
	}
	return false;
	}
	*/


		/*/
	node <T> * getByPosition(unsigned int position)
	{
		if (position <= size){
			node <T> * tmp = start;
			if (position == size)
			{
				tmp = end;

			}
			else
			{
				unsigned int counter = 0;
				node <T> * tmp;
				tmp = start;
				
				while (counter < position)
				{
					tmp = tmp->next;
					counter++;
				}
			}
			return tmp;
		}
	}
	*/